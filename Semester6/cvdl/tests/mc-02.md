# Multiple Choice — Test 02 (from actual lectures)

This test is grounded in the lecture content (all 11 lectures + EfficientNet). 30 questions covering the topic banks the teacher named: basic DL layers, CNN architectures, optimization, evaluation metrics, loss functions, transformers, segmentation, visualization, object detection, SSL, multimodal.

Pick **one** answer per question. Write your choice on the `Answer:` line (e.g. `Answer: C`). Tell me when done and I'll grade in-file.

If you know which topic banks the teacher bolded (higher weight), tell me — I'll re-weight the next test.

---

## Basic layers in DL

**1. Why are activation functions needed between linear layers?**
- A) To make the model harder to overfit
- B) Without a non-linearity, a stack of linear layers collapses into a single linear function
- C) They reduce the number of parameters in the network
- D) They are only needed in the final layer

Answer: ______

**2. What is the "dying ReLU" problem?**
- A) ReLU produces values too large during inference
- B) Neurons can get stuck outputting 0 permanently because the gradient is 0 for negative inputs
- C) ReLU has the same gradient everywhere, causing oscillations
- D) ReLU cannot be used after batch normalization

Answer: ______

**3. Batch normalization works by:**
- A) Normalizing the final output of the network
- B) Forcing pre-activations of each layer to have zero mean and unit variance across the batch, then allowing the network to scale and shift them
- C) Replacing the activation function entirely
- D) Dropping random neurons during training

Answer: ______

**4. The key difference between batch normalization and layer normalization is that:**
- A) Batch norm normalizes across the batch dimension; layer norm normalizes across the feature dimension and does not depend on batch size
- B) Layer norm is only used in CNNs; batch norm only in transformers
- C) Batch norm has no learnable parameters; layer norm does
- D) Layer norm is applied after the activation; batch norm before

Answer: ______

---

## CNN architectures

**5. The main "trick" that makes a stack of two 3×3 convolutions in VGG attractive compared to one 5×5 is:**
- A) Larger receptive field with fewer parameters and an extra non-linearity in between
- B) Smaller receptive field but cheaper
- C) Same number of parameters, just easier to implement
- D) Lets the network skip pooling

Answer: ______

**6. The Inception module's core idea is to:**
- A) Replace convolutions with attention
- B) Apply multiple filter sizes (1×1, 3×3, 5×5) and pooling in parallel and concatenate their outputs along the channel dimension
- C) Use only depthwise separable convolutions
- D) Stack residual blocks of equal depth

Answer: ______

**7. ResNet's residual (skip) connection helps because:**
- A) It removes the need for batch normalization
- B) It makes it easy for a block to learn the identity mapping and lets gradients flow through deep networks, alleviating vanishing gradients
- C) It reduces the number of parameters
- D) It replaces the need for activation functions

Answer: ______

**8. In MobileNet, a depthwise separable convolution replaces a standard convolution with:**
- A) A 1×1 convolution only
- B) A depthwise convolution (one filter per input channel, no cross-channel mixing) followed by a 1×1 pointwise convolution that mixes channels
- C) A group convolution with one group
- D) A transposed convolution

Answer: ______

**9. EfficientNet's compound scaling jointly scales which three dimensions of a CNN?**
- A) Batch size, learning rate, number of epochs
- B) Depth, width, and input resolution
- C) Number of classes, filter size, and stride
- D) Number of GPUs, memory, and FLOPs

Answer: ______

---

## Optimization

**10. Compared to plain SGD, the Adam optimizer:**
- A) Has no learning rate
- B) Maintains running estimates of the first and second moments of the gradient and adapts the per-parameter learning rate
- C) Always converges to the global minimum
- D) Uses larger batches by definition

Answer: ______

**11. The purpose of gradient descent with momentum is to:**
- A) Reduce the memory required by the optimizer
- B) Use an exponentially weighted average of gradients to dampen oscillations and accelerate progress along consistent directions
- C) Replace the need for a learning rate
- D) Eliminate the need for backpropagation

Answer: ______

**12. The key difference between Adam and AdamW is that AdamW:**
- A) Removes momentum entirely
- B) Decouples weight decay from the gradient update, subtracting the weight penalty separately after the Adam step rather than folding it into the gradient
- C) Uses no second-moment estimate
- D) Replaces softmax with sigmoid

Answer: ______

**13. Linear warmup at the start of training is used because:**
- A) Larger learning rates always work better
- B) At init, weights are random and gradients are noisy, so starting from a small LR avoids destructive updates and gives the model time to stabilize
- C) It reduces the number of parameters
- D) It is required for batch normalization

Answer: ______

---

## Loss functions

**14. Cross-entropy loss is the standard loss function for:**
- A) Regression
- B) Classification
- C) Clustering
- D) Dimensionality reduction

Answer: ______

**15. Focal loss is designed to:**
- A) Replace cross-entropy entirely on balanced datasets
- B) Down-weight easy examples (via a modulating factor) so the model focuses gradient updates on hard ones
- C) Add an L2 penalty to the weights
- D) Force all logits to be positive

Answer: ______

**16. Dice loss is most useful in segmentation when:**
- A) Classes are perfectly balanced
- B) Classes are heavily imbalanced (e.g. small foreground object on a huge background); it directly rewards mask overlap
- C) The dataset has only one class
- D) The model is fully untrained

Answer: ______

**17. Hinge (SVM) loss is "happy" when:**
- A) All scores are exactly zero
- B) The score of the correct class is at least a margin m larger than the scores of the other classes
- C) The softmax output is uniform
- D) The bounding box IoU is above 0.5

Answer: ______

---

## Evaluation metrics

**18. In a heavily imbalanced binary problem (e.g. 90% benign / 10% malignant), plain accuracy is misleading because:**
- A) It cannot be computed for binary problems
- B) A trivial model that predicts the majority class everywhere can still score very high while being clinically useless
- C) It requires probabilities, not labels
- D) It only works in multi-class settings

Answer: ______

**19. In a multi-class setting, the difference between macro and micro averaging is:**
- A) Macro computes a metric per class and averages them (treats all classes equally); micro aggregates all TP/FP/FN first and then computes the metric (treats every sample equally)
- B) Macro is for binary problems, micro for multi-class
- C) Micro is always higher than macro
- D) Macro ignores false negatives

Answer: ______

**20. The AUC of an ROC curve measures:**
- A) The maximum precision achievable
- B) The area under the curve plotting True Positive Rate vs False Positive Rate — a threshold-independent summary of classifier performance
- C) The pixel accuracy of segmentation masks
- D) The number of training epochs

Answer: ______

**21. Mean Average Precision (mAP) in object detection is computed as:**
- A) The average of pixel accuracy across classes
- B) The mean of the per-class Average Precision (area under the precision-recall curve), where a detection is a TP if its IoU with a ground-truth box exceeds a threshold
- C) The average number of objects per image
- D) The mean IoU across all classes

Answer: ______

---

## Transformers

**22. In self-attention the input X (shape B×T×D) is projected into queries, keys and values. The attention weights are then computed as:**
- A) `softmax(Q + K^T) · V`
- B) `softmax(Q · K^T / √D) · V`
- C) `sigmoid(Q · V^T) · K`
- D) `Q · K · V`

Answer: ______

**23. The scaling factor √D inside `softmax(Q · K^T / √D)` exists for:**
- A) Numerical stability — without scaling, dot products grow with D and push softmax into very saturated (one-hot-like) regimes with vanishing gradients
- B) Reducing the parameter count
- C) Removing the need for positional information
- D) Increasing the receptive field

Answer: ______

**24. Multi-head attention is implemented by:**
- A) Stacking attention layers sequentially
- B) Running several Q/K/V projections in parallel, each on a lower-dimensional slice of the embedding, then concatenating their outputs and projecting back
- C) Repeating the same single-head attention several times with the same weights
- D) Replacing softmax with multiple sigmoids

Answer: ______

**25. In a Vision Transformer (ViT), positional embeddings are needed because:**
- A) They speed up the softmax
- B) Self-attention is permutation-invariant — without positional info, swapping two patches would give the same output, so the model must be told where each patch came from
- C) They reduce the number of parameters
- D) They replace the need for batch normalization

Answer: ______

**26. The Swin Transformer reduces self-attention cost relative to a vanilla ViT by:**
- A) Removing self-attention entirely
- B) Computing self-attention only within non-overlapping local windows, then alternating with shifted windows so information can flow across window boundaries
- C) Using only one attention head
- D) Replacing softmax with sigmoid

Answer: ______

---

## Segmentation

**27. U-Net's skip connections serve to:**
- A) Reduce overfitting
- B) Pass high-resolution feature maps from the encoder directly to the corresponding decoder level, preserving spatial detail that pooling would otherwise lose
- C) Replace the need for pooling layers
- D) Implement self-attention

Answer: ______

**28. A transposed convolution (`nn.ConvTranspose2d`) is used in segmentation decoders to:**
- A) Compress the feature map further
- B) Provide learned upsampling — the filter weights are trained, unlike nearest-neighbour or bilinear interpolation which have no learnable parameters
- C) Apply a non-linearity to the input
- D) Convert the input to grayscale

Answer: ______

**29. DeepLabV3's atrous (dilated) convolution and ASPP module are used to:**
- A) Reduce the number of parameters
- B) Enlarge the receptive field without losing spatial resolution, and capture multi-scale context by running atrous convolutions with different dilation rates in parallel
- C) Replace the encoder backbone with a transformer
- D) Compute the loss function

Answer: ______

---

## Visualization / interpretability

**30. Grad-CAM produces:**
- A) A 3D rendering of the model
- B) A class-specific heatmap by weighting the feature maps of a chosen conv layer with the gradient of the predicted class score with respect to those feature maps
- C) The confusion matrix
- D) The training loss curve

Answer: ______

**31. The LIME explanation method works by:**
- A) Inspecting model weights directly
- B) Perturbing the input (e.g. hiding super-pixels), observing how the prediction changes, and fitting a simple interpretable model locally around one prediction
- C) Backpropagating to the input pixels
- D) Computing the SHAP values exactly

Answer: ______

---

## Object detection

**32. Non-Maximum Suppression (NMS) is used during inference to:**
- A) Train the detector
- B) Suppress duplicate bounding boxes covering the same object — sort boxes by score, keep the highest-scoring, and discard boxes whose IoU with it exceeds a threshold; repeat
- C) Generate anchor boxes
- D) Compute mAP

Answer: ______

**33. The key change Faster R-CNN introduces over Fast R-CNN is:**
- A) It removes ROI pooling
- B) It replaces the external selective-search proposals with a learned Region Proposal Network (RPN) that predicts proposals from CNN feature maps
- C) It removes the bounding-box regressor
- D) It eliminates anchor boxes entirely

Answer: ______

**34. YOLO is a "one-stage" detector. Its core formulation is:**
- A) First propose regions, then classify them in a second stage
- B) Divide the image into an S×S grid; for each cell predict B bounding boxes (coords + objectness) and C class probabilities — all in one forward pass
- C) Use a transformer encoder over patches
- D) Use a sliding window classifier over the full image

Answer: ______

**35. DETR (Detection Transformer) differs from R-CNN/YOLO in that it:**
- A) Uses sliding windows
- B) Formulates detection as a direct set prediction: a fixed number N of learned object queries are decoded in parallel, and predictions are matched to ground-truth boxes via bipartite matching (Hungarian)
- C) Removes the CNN backbone
- D) Does not use attention

Answer: ______

---

## Self-supervised learning (SSL)

**36. The InfoNCE loss used by contrastive methods (e.g. MoCo, SimCLR) is essentially:**
- A) Mean squared error between two embeddings
- B) A softmax cross-entropy that frames learning as a (K+1)-way classification: pull the query close to its positive key and push it away from K negatives
- C) Binary cross-entropy between mask probabilities
- D) Dice loss applied on embeddings

Answer: ______

**37. The Masked Autoencoder (MAE) approach to self-supervised vision pretraining works by:**
- A) Pulling together two augmented views of the same image
- B) Masking a large proportion of image patches (e.g. 75%), encoding only the visible ones with a ViT, and using a lightweight decoder to reconstruct the original pixels of the missing patches
- C) Predicting the class of each patch
- D) Training a contrastive loss between text and image

Answer: ______

---

## Multimodal

**38. CLIP is trained to:**
- A) Generate images from text only
- B) Align image and text representations in a shared embedding space using a contrastive loss over (image, caption) pairs — pull matched pairs together, push non-matched apart
- C) Translate between natural languages
- D) Segment any object in an image given a click

Answer: ______

**39. SAM (Segment Anything Model) is a foundation model for segmentation that:**
- A) Trains a separate model per dataset
- B) Takes an image plus a prompt (points, boxes, text) and outputs a valid segmentation mask, predicting multiple candidate masks (whole / part / subpart) to handle prompt ambiguity
- C) Only works on medical images
- D) Replaces self-attention with convolutions

Answer: ______

**40. BLIP-2's Q-Former is best described as:**
- A) A standalone classifier for ImageNet
- B) A lightweight bridge between a frozen image encoder and a frozen LLM: a small set of learnable query embeddings extract image features via cross-attention and feed them to the LLM as soft visual prompts
- C) A replacement for the LLM
- D) A new optimizer designed for vision-language training

Answer: ______

---

When done, ping me. I'll grade in-file the same way as the basics tests.
